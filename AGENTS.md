# Repository context

Read `.defs/terms.toml` and `.defs/contracts.toml` before changing domain
vocabulary or guaranteed behavior. Update the corresponding entries in the
same change whenever either contract surface changes.
